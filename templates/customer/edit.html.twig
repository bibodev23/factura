{% extends 'base.html.twig' %}

{% block title %}Mettre à jour Customer{% endblock %}

{% block body %}
    <h1>Mettre à jour {{ customer.name }} </h1>

    {{ include('customer/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_customer_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
