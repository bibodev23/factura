<form method="post" action="{{ path('app_my_company_delete', {'id': my_company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ my_company.id) }}">
    <button class="btn">Delete</button>
</form>
