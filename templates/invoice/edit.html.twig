{% extends 'base.html.twig' %}

{% block title %}Mettre à jour Invoice{% endblock %}

{% block body %}
    <h1>Mettre à jour cette facture</h1>

    {{ component('InvoiceForm', {invoice: invoice})}}

    <a href="{{ path('app_invoice_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('invoice/_delete_form.html.twig') }}
{% endblock %}
